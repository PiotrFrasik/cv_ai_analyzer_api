# cv_analyzer
CV analyzer - the user uploads a CV (PDF), and the system parses it and compares it with the job offer, returning matches and suggestions. It pairs nicely with your CV because the design itself is "meta".
